<div class="project rounded-lg shadow-xl">
  <slot />
</div>

<style>
  .project {
    position: relative;
    overflow: hidden;
    transition: transform 0.3s ease;
  }

  .project:hover {
    transform: scale(1.03);
  }

  @media only screen and (max-width: 768px) {
    .project {
      margin: 18px 0;
    }

    .project:hover {
      transform: none;
    }
  }
</style>
